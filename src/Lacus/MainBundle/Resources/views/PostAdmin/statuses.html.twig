<div class="btn-group list-status">
    <a href="{{ path('admin_lacus_main_post_set_status',{'post':post.id,'status':'draft','token':csrf_token('set-status')}) }}" class="set-post-status btn btn-mini {% if post.status == 'draft' %}btn-inverse active{% endif %} {% if not admin.canSetStatus(post, 'draft') %}disabled{% endif %}">Draft</a>
    <a href="{{ path('admin_lacus_main_post_set_status',{'post':post.id,'status':'review','token':csrf_token('set-status')}) }}" class="set-post-status btn btn-mini {% if post.status == 'review' %}btn-warning active{% endif %} {% if not admin.canSetStatus(post, 'review') %}disabled{% endif %}">Review</a>
    <a href="{{ path('admin_lacus_main_post_set_status',{'post':post.id,'status':'queue','token':csrf_token('set-status')}) }}" class="set-post-status btn btn-mini {% if post.status == 'queue' %}btn-info active{% endif %} {% if not admin.canSetStatus(post, 'queue') %}disabled{% endif %}">Queue</a>
    <a href="{{ path('admin_lacus_main_post_set_status',{'post':post.id,'status':'publish','token':csrf_token('set-status')}) }}" class="set-post-status btn btn-mini {% if post.status == 'publish' %}btn-primary active{% endif %} {% if not admin.canSetStatus(post, 'publish') %}disabled{% endif %}">Published</a>
    <a href="{{ path('admin_lacus_main_post_set_status',{'post':post.id,'status':'archive','token':csrf_token('set-status')}) }}" class="set-post-status btn btn-mini {% if post.status == 'archive' %}btn-success active{% endif %} {% if not admin.canSetStatus(post, 'archive') %}disabled{% endif %}">Archived</a>
    <a href="{{ path('admin_lacus_main_post_set_status',{'post':post.id,'status':'failure','token':csrf_token('set-status')}) }}" class="set-post-status btn btn-mini {% if post.status == 'failure' %}btn-danger active{% endif %} {% if not admin.canSetStatus(post, 'failure') %}disabled{% endif %}">Failure</a>
</div>